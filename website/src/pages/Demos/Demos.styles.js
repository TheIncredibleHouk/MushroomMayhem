import styled from "styled-components";

var DemoStyles = styled.div`
    .demos-layout {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
    }
`;

export default DemoStyles;